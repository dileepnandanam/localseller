= render partial: 'profile_sub_nav'
.open-bids-container
  - @product_bids.each do |product, bids|
    %h5= product.name
    - bids.each do |bid| 
      = render partial: '/bids/bid', locals: {bid: bid, seller: true}
