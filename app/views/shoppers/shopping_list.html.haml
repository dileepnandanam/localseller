%h2
  Purchase history

- @shopping_carts.each do |cart|
  .shoping-cart-history
    %table.table
      %th
        purchased date
      %th
        item
      %th
        shop
      %th
        shipped
      %th
        Contact number
        
      - cart.purchases.each  do |purchase|
        %tr
          %td=purchase.created_at.strftime("%Y/%m/%d")
          %td=purchase.product.name
          %td=link_to purchase.shop.name, shop_path(purchase.shop)
          %td=purchase.shiped ? 'Shipped' : 'pending'
          %td=purchase.shop.phone_number
  %br