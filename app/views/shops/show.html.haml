.shop-details
  = image_tag(@shop.banner.url)
  .shop-detail-name
    = @shop.name
  .shop-detail-description
    = @shop.description
= react_component 'Products', {products_url:  for_shop_products_path(permalink: @shop.permalink) , shoping_cart_url: shoping_carts_path, current_user: current_user.present?, per_page: 10}